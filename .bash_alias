alias ls='ls -Ga'
alias c='clear'
alias e='exit'
alias reload='source ~/.zshrc'
alias tree='tree -C'
alias tmux='TERM=screen-256color-bce tmux'

# mkdir, cd into it
function mkcd {
  mkdir $1
  cd $1
}
